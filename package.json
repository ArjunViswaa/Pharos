{
  "name": "pharos",
  "version": "0.1.0",
  "private": true,
  "description": "Pharos - a marketplace where learners book paid 1-on-1 sessions with verified mentors",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
